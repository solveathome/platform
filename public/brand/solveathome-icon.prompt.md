# solveathome icon master

Generated using the built-in image generation tool in edit mode, with
`solveathome-logo.png` as the source image.

## Final prompt

Use case: precise-object-edit. Asset type: square master icon for GitHub avatars, app icons, and favicon resizing. Input image 1 is the edit target: the approved solveathome logo. Extract and reuse ONLY the exact interlocking two-piece S symbol at the left of the source image; remove the entire solveathome wordmark. Keep the symbol's geometry, angles, rounded corners, weight, negative space, and proportions unchanged. Create a 1024x1024 square canvas with a completely flat solid off-white #fbfaf7 background extending fully to all four edges. Render the symbol as solid charcoal #1a1a1a, centered both horizontally and vertically, occupying approximately 62 percent of the square's height. Preserve generous safe space around the mark for circular GitHub avatar and maskable app icon cropping. Crisp antialiased graphic edges. No text, wordmark, new shapes, outer rounded tile, border, shadow, gradient, texture, lighting, or dimensional effects. The only changes to the input are removing the wordmark, centering and scaling the exact existing symbol on the square, and applying the specified solid colors.

Actual generated dimensions: 1254 × 1254. Exported to standard upload and browser sizes
by `scripts/build-icons.mjs` using ImageMagick.
